[pytest]
asyncio_mode = auto
markers =
    unit: pure logic tests with no external dependencies
    integration: tests requiring a running server, database, or network
    slow: tests that take significant time to run
